# Dependency directories (ไม่ต้องเอาเข้า เพราะเราสั่ง install ใน Docker)
node_modules/
jspm_packages/

# Build output (ไม่ต้องเอาเข้า เพราะเราสั่ง build ใหม่ใน Docker)
dist/
build/
.cache/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files
.env
.env.local
.env.sample

# Git
.git
.gitignore

# Docker (ตัวมันเองไม่ต้องก๊อปปี้เข้าไปข้างใน)
Dockerfile
.dockerignore

# OS files
.DS_Store
Thumbs.db

# Unnecessary files
README.md
LICENSE